{
  "name": "intelligent-fuel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cavite thesis prototype — development environment and synthetic fixture contracts",
  "engines": {
    "node": ">=24.15.0 <25",
    "npm": ">=11 <12"
  },
  "scripts": {
    "setup": "tsx scripts/setup.ts",
    "dev": "node --env-file=.env --import tsx --watch src/server.ts",
    "start": "node --env-file=.env dist/src/server.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src scripts tests public/app.js package.json tsconfig.json tsconfig.build.json biome.json",
    "format": "biome check --write src scripts tests public/app.js package.json tsconfig.json tsconfig.build.json biome.json",
    "test": "tsx --test tests/*.test.ts",
    "fixtures:check": "tsx scripts/check-fixtures.ts",
    "prices:check": "tsx scripts/check-price-staging.ts",
    "check": "npm run lint && npm run typecheck && npm test && npm run fixtures:check && npm run prices:check && npm run build"
  },
  "dependencies": {
    "better-auth": "1.7.4",
    "decimal.js": "10.6.0",
    "fastify": "5.12.4",
    "zod": "4.6.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "@types/node": "24.13.4",
    "tsx": "4.23.13",
    "typescript": "7.0.2"
  }
}
